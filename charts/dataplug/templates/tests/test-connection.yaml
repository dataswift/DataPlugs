apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dataplug.fullname" . }}-test-connection"
  labels:
    {{- include "dataplug.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dataplug.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
